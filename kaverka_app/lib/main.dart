import 'package:flutter/material.dart';

void main() => runApp(KaverkaApp());

class KaverkaApp extends StatelessWidget {
  const KaverkaApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
